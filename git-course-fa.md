# 📘 آموزش کامل Git از صفر تا صد (Zero to Hero)

> راهنمای جامع، عملی و حرفه‌ای برای یادگیری Git — از اولین دستور تا مفاهیم پیشرفته‌ای که در محیط کار واقعی به کارتان می‌آید.

---

## 📑 فهرست مطالب

1. [Git چیست و چرا باید یاد بگیریم](#بخش-1)
2. [نصب و پیکربندی اولیه](#بخش-2)
3. [مفاهیم پایه‌ای که باید قبل از هر چیز بدانید](#بخش-3)
4. [شروع کار: ساخت اولین ریپازیتوری](#بخش-4)
5. [چرخه‌ی اصلی کار با Git (Add, Commit, Status, Log)](#بخش-5)
6. [بررسی تغییرات (Diff) و تاریخچه](#بخش-6)
7. [نادیده گرفتن فایل‌ها با gitignore.](#بخش-7)
8. [بازگرداندن و لغو تغییرات](#بخش-8)
9. [شاخه‌ها (Branching) به زبان ساده](#بخش-9)
10. [ادغام شاخه‌ها (Merge)](#بخش-10)
11. [حل تعارض (Merge Conflict)](#بخش-11)
12. [Rebase: جایگزین حرفه‌ای Merge](#بخش-12)
13. [کار با ریموت‌ها (GitHub, GitLab و...)](#بخش-13)
14. [Clone، Fetch، Pull، Push](#بخش-14)
15. [Git Stash](#بخش-15)
16. [برچسب‌ها (Tags)](#بخش-16)
17. [Cherry-pick](#بخش-17)
18. [Reflog: ماشین زمان Git](#بخش-18)
19. [Bisect: پیدا کردن باگ با جست‌وجوی دودویی](#بخش-19)
20. [Submodules](#بخش-20)
21. [Git Hooks](#بخش-21)
22. [درون Git چه خبر است؟ (معماری داخلی)](#بخش-22)
23. [استانداردهای پیام کامیت (Commit Message)](#بخش-23)
24. [ورک‌فلوهای حرفه‌ای تیمی (Git Flow, GitHub Flow, Trunk-Based)](#بخش-24)
25. [Pull Request و بررسی کد (Code Review)](#بخش-25)
26. [آشنایی با GitHub Actions (CI/CD مقدماتی)](#بخش-26)
27. [دستورات و تنظیمات پیشرفته و کاربردی](#بخش-27)
28. [اشتباهات رایج و راه‌حل آن‌ها](#بخش-28)
29. [Cheat Sheet نهایی](#بخش-29)
30. [منابع بیشتر برای یادگیری عمیق‌تر](#بخش-30)

---

<a name="بخش-1"></a>
## 1️⃣ Git چیست و چرا باید یاد بگیریم؟

### تعریف ساده
Git یک **سیستم کنترل نسخه توزیع‌شده (Distributed Version Control System - DVCS)** است. یعنی نرم‌افزاری که تاریخچه‌ی تمام تغییرات یک پروژه (معمولاً کد) را ثبت می‌کند، طوری که:

- می‌توانید به هر نسخه‌ی قبلی پروژه برگردید.
- می‌توانید بفهمید **چه کسی**، **چه زمانی** و **چرا** یک تغییر را ایجاد کرده.
- چند نفر می‌توانند **همزمان** روی یک پروژه کار کنند بدون این‌که کار همدیگر را خراب کنند.
- یک نسخه‌ی کامل از کل تاریخچه‌ی پروژه روی سیستم هر توسعه‌دهنده وجود دارد (به همین دلیل به آن «توزیع‌شده» می‌گویند).

### چرا Git و نه ابزار دیگر؟
Git توسط **لینوس توروالدز** (خالق لینوکس) در سال ۲۰۰۵ برای مدیریت کد کرنل لینوکس ساخته شد. امروز:

- استاندارد صنعت نرم‌افزار است.
- پایه‌ی پلتفرم‌هایی مثل **GitHub**, **GitLab**, **Bitbucket** است.
- سریع، سبک و کاملاً افلاین (بدون نیاز به اینترنت برای اکثر عملیات) کار می‌کند.
- در تقریباً هر آگهی استخدام برنامه‌نویسی به عنوان مهارت پایه ذکر می‌شود.

### تفاوت Git و GitHub — یک سوءتفاهم رایج
این دو را با هم اشتباه نگیرید:

| | Git | GitHub |
|---|---|---|
| چیست؟ | یک **نرم‌افزار** برای کنترل نسخه | یک **سرویس آنلاین** برای میزبانی ریپازیتوری‌های Git |
| کجا اجرا می‌شود؟ | روی سیستم شما (لوکال) | روی سرورهای مایکروسافت (ابری) |
| جایگزین دارد؟ | Mercurial, SVN | GitLab, Bitbucket, Gitea |
| نیاز به اینترنت؟ | خیر (برای اکثر کارها) | بله |

به زبان ساده: **Git موتور است، GitHub پارکینگ آنلاینی است که ماشین‌ها (پروژه‌ها) را در آن نگه می‌دارید و به اشتراک می‌گذارید.**

### چه زمانی از Git استفاده کنیم؟
- تقریباً همیشه! حتی برای پروژه‌های شخصی تک‌نفره.
- برای نوشتن مقاله، مستندات، تنظیمات (dotfiles)، و نه فقط کد.

---
<a name="بخش-2"></a>
## 2️⃣ نصب و پیکربندی اولیه

### نصب Git

**در لینوکس (Debian/Ubuntu/Mint):**
```bash
sudo apt update
sudo apt install git
```

**در لینوکس (Fedora/RedHat):**
```bash
sudo dnf install git
```

**در Arch Linux:**
```bash
sudo pacman -S git
```

**در macOS:**
```bash
brew install git
```

**در ویندوز:**
از سایت رسمی [git-scm.com](https://git-scm.com/download/win) دانلود و نصب کنید (شامل Git Bash می‌شود).

بررسی نصب موفق:
```bash
git --version
```

### پیکربندی هویت (اجباری - قبل از اولین commit)

Git باید بداند شما چه کسی هستید تا در هر commit این اطلاعات را ثبت کند:

```bash
git config --global user.name "Amir"
git config --global user.email "your_email@example.com"
```

توضیح فلگ `--global`: یعنی این تنظیمات برای **همه‌ی** ریپازیتوری‌های روی سیستم شما اعمال می‌شود. اگر بخواهید فقط برای یک پروژه‌ی خاص ایمیل دیگری تنظیم کنید (مثلاً یک ایمیل کاری جدا)، از دستور بدون `--global` داخل همان پوشه استفاده کنید:

```bash
git config user.email "work_email@example.com"
```

### تنظیمات مفید دیگر

**تنظیم ادیتور پیش‌فرض (مثلاً nvim):**
```bash
git config --global core.editor "nvim"
```

**رنگی کردن خروجی دستورات (خوانایی بهتر):**
```bash
git config --global color.ui auto
```

**تنظیم نام شاخه‌ی پیش‌فرض روی main (استاندارد جدید):**
```bash
git config --global init.defaultBranch main
```

**دیدن همه‌ی تنظیمات فعلی:**
```bash
git config --list
```

**دیدن یک تنظیم خاص:**
```bash
git config user.name
```

**محل ذخیره‌سازی تنظیمات:**
- `--global` → فایل `~/.gitconfig`
- بدون فلگ (local) → فایل `.git/config` داخل هر پروژه
- `--system` → تنظیمات سراسری سیستم (به‌ندرت استفاده می‌شود)

### ساخت Alias برای دستورات پرکاربرد (میان‌بر)

برای صرفه‌جویی در تایپ:
```bash
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.lg "log --oneline --graph --all --decorate"
```
حالا به‌جای `git status` کافی است بنویسید `git st`.

---

<a name="بخش-3"></a>
## 3️⃣ مفاهیم پایه‌ای که باید قبل از هر چیز بدانید

قبل از اجرای هر دستوری، باید این مفاهیم را کاملاً درک کنید — چون **۹۰٪ سردرگمی مبتدیان** از نفهمیدن همین‌هاست.

### سه ناحیه‌ی اصلی Git

Git فایل‌های شما را در سه «مکان» مختلف نگه می‌دارد:

```
┌─────────────────┐      git add       ┌─────────────────┐      git commit      ┌─────────────────┐
│  Working         │  ───────────────>  │  Staging Area    │  ─────────────────>  │  Repository       │
│  Directory        │                    │  (Index)          │                       │  (.git folder)     │
│  (فایل‌های واقعی)  │  <───────────────  │  (منطقه‌ی آماده‌سازی) │  <─────────────────  │  (تاریخچه‌ی commit‌ها) │
└─────────────────┘   git restore      └─────────────────┘    git reset         └─────────────────┘
```

1. **Working Directory (دایرکتوری کاری):**
   فایل‌هایی که روی سیستم خودتان می‌بینید و ویرایش می‌کنید. همان پوشه‌ی پروژه.

2. **Staging Area (منطقه‌ی آماده‌سازی / Index):**
   یک ناحیه‌ی میانی. وقتی فایلی را ویرایش می‌کنید، تا زمانی که آن را با `git add` به staging اضافه نکنید، Git آن را برای commit در نظر نمی‌گیرد. این به شما اجازه می‌دهد **انتخاب کنید** کدام تغییرات وارد commit بعدی شوند (مثلاً فقط ۲ فایل از ۵ فایل تغییر یافته).

3. **Repository (مخزن / `.git`):**
   جایی که تاریخچه‌ی دائمی و ثبت‌شده‌ی پروژه (همه‌ی commit‌ها) نگهداری می‌شود. وقتی `git commit` می‌زنید، محتوای staging به‌صورت یک «عکس فوری» (snapshot) دائمی در اینجا ذخیره می‌شود.

### تشبیه ساده (برای درک بهتر)
فرض کنید در حال نوشتن یک کتاب هستید:
- **Working Directory** = میز کارتان، جایی که برگه‌ها را می‌نویسید و اصلاح می‌کنید.
- **Staging Area** = پوشه‌ای که برگه‌های نهایی‌شده را قبل از چاپ در آن می‌گذارید («این‌ها آماده‌ی چاپ‌اند»).
- **Commit** = لحظه‌ی چاپ و صحافی آن نسخه به‌عنوان یک فصل رسمی از کتاب — دیگر تغییر نمی‌کند مگر با نسخه‌ی جدید.

### واژه‌نامه‌ی ضروری (قبل از ادامه حفظ کنید)

| اصطلاح | معنی |
|---|---|
| **Repository (Repo)** | پروژه‌ای که تحت کنترل Git است؛ شامل پوشه‌ی مخفی `.git` |
| **Commit** | یک عکس فوری (snapshot) ثبت‌شده از وضعیت فایل‌ها در یک لحظه‌ی خاص |
| **Branch (شاخه)** | یک خط مستقل از توسعه؛ به شما اجازه می‌دهد بدون تأثیر روی کد اصلی، کار کنید |
| **HEAD** | اشاره‌گری که نشان می‌دهد شما «الان» روی کدام commit/شاخه ایستاده‌اید |
| **Remote** | نسخه‌ای از ریپازیتوری که روی یک سرور دیگر (مثل GitHub) قرار دارد |
| **Clone** | کپی کردن کامل یک ریپازیتوری از یک remote به سیستم خودتان |
| **Origin** | نام پیش‌فرضی که Git به remote اصلی شما می‌دهد |
| **Merge** | ادغام تغییرات یک شاخه در شاخه‌ی دیگر |
| **Conflict** | زمانی که Git نمی‌تواند به‌طور خودکار دو تغییر متناقض را ادغام کند |
| **Hash (SHA-1)** | شناسه‌ی یکتای ۴۰ کاراکتری هر commit، مثل `a3f5c9d...` |

---
<a name="بخش-4"></a>
## 4️⃣ شروع کار: ساخت اولین ریپازیتوری

### روش اول: شروع یک پروژه‌ی جدید از صفر

```bash
mkdir my-project
cd my-project
git init
```

دستور `git init` یک پوشه‌ی مخفی به نام `.git` می‌سازد. **همه‌چیز** درباره‌ی تاریخچه‌ی پروژه‌ی شما در همین پوشه ذخیره می‌شود. اگر این پوشه را حذف کنید، کل تاریخچه‌ی Git از بین می‌رود (فایل‌های فعلی‌تان دست‌نخورده باقی می‌مانند).

بررسی این‌که آیا الان داخل یک ریپازیتوری Git هستید یا نه:
```bash
git status
```

### روش دوم: کپی کردن یک ریپازیتوری موجود (مثلاً از GitHub)

```bash
git clone https://github.com/username/repo-name.git
```

این دستور:
1. کل تاریخچه‌ی commit‌ها را دانلود می‌کند.
2. یک پوشه به نام `repo-name` می‌سازد.
3. آخرین نسخه‌ی فایل‌ها را در آن قرار می‌دهد.
4. به‌طور خودکار remote به نام `origin` تنظیم می‌کند.

اگر بخواهید نام پوشه را خودتان انتخاب کنید:
```bash
git clone https://github.com/username/repo-name.git my-folder-name
```

---

<a name="بخش-5"></a>
## 5️⃣ چرخه‌ی اصلی کار با Git

این بخش، **قلب Git** است. حداقل ۸۰٪ کارهای روزمره‌ی شما همین چرخه است.

### مرحله ۱: بررسی وضعیت فعلی
```bash
git status
```
نشان می‌دهد:
- کدام فایل‌ها تغییر کرده‌اند ولی هنوز stage نشده‌اند (رنگ قرمز).
- کدام فایل‌ها stage شده و آماده‌ی commit‌اند (رنگ سبز).
- کدام فایل‌ها جدید و «رهگیری‌نشده» (untracked) هستند.

### مرحله ۲: اضافه کردن فایل‌ها به Staging Area
```bash
git add filename.txt          # اضافه کردن یک فایل خاص
git add file1.txt file2.txt   # چند فایل
git add .                     # همه‌ی فایل‌های تغییر یافته در پوشه‌ی فعلی و زیرپوشه‌ها
git add -A                    # همه‌ی فایل‌ها در کل ریپازیتوری (شامل حذف‌شده‌ها)
git add -p                    # حالت تعاملی: انتخاب بخش‌به‌بخش تغییرات داخل یک فایل (بسیار حرفه‌ای)
```

💡 نکته‌ی حرفه‌ای: `git add -p` به شما اجازه می‌دهد فقط **بخشی** از تغییرات یک فایل را commit کنید. این برای جدا کردن تغییرات نامرتبط در یک commit تمیز، بسیار کاربردی است.

### مرحله ۳: ثبت Commit
```bash
git commit -m "پیام توضیح‌دهنده‌ی تغییرات"
```

میان‌بر برای add + commit فایل‌های **قبلاً رهگیری‌شده** (untracked جدید را شامل نمی‌شود):
```bash
git commit -am "پیام کامیت"
```

اگر پیام commit طولانی‌تر می‌خواهید (بدون فلگ `-m`، ادیتور پیش‌فرض باز می‌شود):
```bash
git commit
```

### مرحله ۴: مشاهده‌ی تاریخچه
```bash
git log
```
خروجی خلاصه‌تر و خواناتر:
```bash
git log --oneline
git log --oneline --graph --all --decorate
```

### خلاصه‌ی چرخه‌ی کامل (نمونه‌ی واقعی)
```bash
echo "# پروژه‌ی من" > README.md
git status                      # می‌بینید README.md به‌عنوان untracked نشان داده می‌شود
git add README.md
git status                      # حالا سبز شده و آماده‌ی commit است
git commit -m "افزودن README اولیه"
git log --oneline               # commit جدید را می‌بینید
```

---

<a name="بخش-6"></a>
## 6️⃣ بررسی تغییرات (Diff) و تاریخچه

### دیدن تغییرات هنوز stage نشده (بین working directory و staging)
```bash
git diff
```

### دیدن تغییرات stage شده (بین staging و آخرین commit)
```bash
git diff --staged
# یا معادل آن:
git diff --cached
```

### مقایسه‌ی دو commit با هم
```bash
git diff commit1_hash commit2_hash
```

### دیدن جزئیات کامل یک commit خاص
```bash
git show a3f5c9d
```

### دیدن تاریخچه‌ی یک فایل خاص
```bash
git log --follow -- filename.txt
```

### دیدن این‌که هر خط از یک فایل، توسط چه کسی و در چه commit‌ای نوشته شده (خیلی مفید در دیباگ)
```bash
git blame filename.txt
```

### فیلتر کردن لاگ
```bash
git log --author="Amir"                 # فقط commit‌های یک نویسنده
git log --since="2 weeks ago"           # فقط ۲ هفته‌ی اخیر
git log --grep="fix"                    # جست‌وجو در پیام‌های commit
git log -p                              # نمایش diff کامل هر commit
git log --stat                          # آمار خلاصه (چند خط اضافه/حذف در هر فایل)
```

---

<a name="بخش-7"></a>
## 7️⃣ نادیده گرفتن فایل‌ها با gitignore.

### چرا به آن نیاز داریم؟
بعضی فایل‌ها نباید وارد تاریخچه‌ی Git شوند:
- فایل‌های تولیدشده توسط کامپایلر یا build (مثل `node_modules/`, `__pycache__/`)
- فایل‌های حساس (رمزها، کلیدهای API، فایل‌های `.env`)
- فایل‌های سیستم‌عامل (`.DS_Store` در مک، `Thumbs.db` در ویندوز)
- فایل‌های شخصی ادیتور (`.vscode/`, `.idea/`)

### نحوه‌ی ساخت
یک فایل به نام دقیق `.gitignore` در ریشه‌ی پروژه بسازید:

```gitignore
# پوشه‌های وابستگی
node_modules/
venv/
__pycache__/

# فایل‌های محیطی و حساس
.env
*.key
secrets.json

# فایل‌های سیستم‌عامل
.DS_Store
Thumbs.db

# فایل‌های ادیتور
.vscode/
.idea/
*.swp

# فایل‌های build
dist/
build/
*.log
```

### قوانین نوشتن الگو (pattern)
| الگو | معنی |
|---|---|
| `file.txt` | نادیده گرفتن دقیقاً این فایل در هر جای پروژه |
| `*.log` | نادیده گرفتن همه‌ی فایل‌های با پسوند `.log` |
| `folder/` | نادیده گرفتن کل یک پوشه |
| `/config.txt` | فقط `config.txt` در ریشه (نه در زیرپوشه‌ها) |
| `!important.log` | استثنا قائل شدن (این فایل را با وجود قانون بالا، نادیده نگیر) |

⚠️ نکته‌ی مهم: `.gitignore` فقط روی فایل‌های **رهگیری‌نشده** اثر می‌گذارد. اگر فایلی از قبل commit شده باشد، اضافه کردنش به `.gitignore` آن را حذف نمی‌کند. برای حذف فایلی که اشتباهاً commit شده:

```bash
git rm --cached filename
git commit -m "حذف فایل از رهگیری Git"
```

---
<a name="بخش-8"></a>
## 8️⃣ بازگرداندن و لغو تغییرات

این یکی از مهم‌ترین بخش‌هایی است که مبتدیان با آن مشکل دارند. **قانون طلایی**: هر دستور، سطح متفاوتی از «لغو کردن» را انجام می‌دهد.

### سطح ۱: لغو تغییرات در Working Directory (هنوز add نشده)
```bash
git restore filename.txt
# روش قدیمی (هنوز کار می‌کند):
git checkout -- filename.txt
```
فایل را به آخرین نسخه‌ی commit‌شده برمی‌گرداند. **تغییرات ذخیره‌نشده برای همیشه از بین می‌روند.**

### سطح ۲: خارج کردن فایل از Staging (بدون از بین بردن تغییرات)
```bash
git restore --staged filename.txt
# روش قدیمی:
git reset filename.txt
```
فایل از staging خارج می‌شود ولی تغییرات در working directory باقی می‌مانند.

### سطح ۳: لغو آخرین Commit

**حالت الف: می‌خواهید commit را حذف کنید ولی تغییرات را نگه دارید (رایج‌ترین حالت)**
```bash
git reset --soft HEAD~1
```
commit حذف می‌شود، ولی همه‌ی تغییرات به‌صورت stage شده در working directory باقی می‌مانند. عالی برای اصلاح پیام commit یا ترکیب چند commit.

**حالت ب: می‌خواهید commit و staging را لغو کنید، ولی فایل‌های working directory دست‌نخورده بمانند**
```bash
git reset --mixed HEAD~1
# (این حالت پیش‌فرض reset است)
git reset HEAD~1
```

**حالت ج: می‌خواهید همه‌چیز از بین برود — commit، staging، و تغییرات در فایل‌ها (⚠️ خطرناک)**
```bash
git reset --hard HEAD~1
```
⚠️ **هشدار جدی**: این دستور غیرقابل بازگشت است (مگر با reflog که بعداً یاد می‌گیرید). قبل از استفاده از `--hard` همیشه دوباره فکر کنید.

### سطح ۴: لغو یک Commit که قبلاً Push شده (روش امن)
اگر commit را به‌اشتراک گذاشته‌اید (push کرده‌اید)، **هرگز از reset استفاده نکنید** — چون تاریخچه را برای بقیه به‌هم می‌ریزد. به‌جای آن از `revert` استفاده کنید:

```bash
git revert commit_hash
```
این دستور یک **commit جدید** می‌سازد که دقیقاً برعکس تغییرات commit هدف را اعمال می‌کند. تاریخچه دست‌نخورده باقی می‌ماند و برای همکاری تیمی امن است.

### تفاوت کلیدی: Reset در برابر Revert

| | `reset` | `revert` |
|---|---|---|
| تاریخچه را بازنویسی می‌کند؟ | بله | خیر |
| امن برای commit‌های push‌شده؟ | ❌ نه | ✅ بله |
| مناسب برای | تمیز کردن commit‌های لوکال قبل از push | لغو تغییراتی که قبلاً به اشتراک گذاشته شده |

### اصلاح آخرین Commit (پیام یا فایل فراموش‌شده)
```bash
git add forgotten_file.txt
git commit --amend -m "پیام جدید و صحیح"
```
⚠️ فقط زمانی از `--amend` استفاده کنید که commit را **هنوز push نکرده‌اید**.

### حذف فایل‌های untracked (فایل‌هایی که هرگز add نشده‌اند)
```bash
git clean -n     # فقط نمایش (dry-run) — چه فایل‌هایی حذف می‌شوند
git clean -f      # حذف واقعی فایل‌ها
git clean -fd     # حذف فایل‌ها و پوشه‌های untracked
```

---

<a name="بخش-9"></a>
## 9️⃣ شاخه‌ها (Branching) به زبان ساده

### چرا شاخه‌بندی مهم است؟
تصور کنید روی یک قابلیت جدید کار می‌کنید که ۲ هفته طول می‌کشد. اگر مستقیماً روی `main` کار کنید، در این مدت کد اصلی پروژه ناپایدار و شکسته خواهد بود. **branch** به شما اجازه می‌دهد یک «کپی موازی» از پروژه بسازید، آزادانه در آن آزمایش و توسعه دهید، و فقط زمانی که کارتان کامل و تست‌شده بود، آن را به `main` برگردانید.

### ساخت شاخه‌ی جدید
```bash
git branch feature-login
```

### دیدن لیست شاخه‌ها
```bash
git branch          # شاخه‌های لوکال
git branch -r        # شاخه‌های remote
git branch -a        # همه (لوکال + remote)
```
شاخه‌ای که علامت `*` کنارش است، شاخه‌ی فعلی شماست.

### جابه‌جایی بین شاخه‌ها
```bash
git checkout feature-login
# روش جدیدتر و توصیه‌شده (از Git 2.23 به بعد):
git switch feature-login
```

### ساخت و جابه‌جایی هم‌زمان
```bash
git checkout -b feature-login
# روش جدید:
git switch -c feature-login
```

### تغییر نام یک شاخه
```bash
git branch -m old-name new-name
```

### حذف یک شاخه
```bash
git branch -d feature-login     # حذف امن (فقط اگر merge شده باشد)
git branch -D feature-login     # حذف اجباری (حتی اگر merge نشده)
```

### گردش کار نمونه با شاخه‌ها
```bash
git switch -c feature-dark-mode   # ساخت شاخه‌ی جدید برای یک قابلیت
# ... کد نویسی و چند commit ...
git switch main                    # برگشت به شاخه‌ی اصلی
git merge feature-dark-mode        # ادغام قابلیت تمام‌شده به main
git branch -d feature-dark-mode    # حذف شاخه‌ی دیگر لازم نیست
```

### قرارداد نام‌گذاری شاخه‌ها (استاندارد صنعتی)
```
feature/login-page
bugfix/navbar-overlap
hotfix/critical-payment-bug
release/v2.1.0
chore/update-dependencies
```

---

<a name="بخش-10"></a>
## 🔟 ادغام شاخه‌ها (Merge)

### Merge چگونه کار می‌کند؟

```bash
git switch main
git merge feature-branch
```

Git دو حالت اصلی ادغام دارد:

**۱. Fast-Forward Merge**
وقتی از زمان جدا شدن شاخه‌ها، هیچ commit جدیدی روی `main` نیامده باشد، Git به‌سادگی اشاره‌گر `main` را جلو می‌برد (بدون ساخت commit جدید).

```
قبل:   main → A
                 \
       feature →  B → C

بعد:   main → A → B → C
```

**۲. Three-Way Merge (ادغام سه‌طرفه)**
وقتی هر دو شاخه commit‌های جدید داشته‌اند، Git یک **commit ادغام (merge commit)** جدید می‌سازد که دو والد دارد.

```
main:     A → B ──────→ M (merge commit)
                          ↗
feature:      C → D ────┘
```

### جلوگیری از fast-forward (برای حفظ تاریخچه‌ی شاخه به‌صورت واضح)
```bash
git merge --no-ff feature-branch
```
این گزینه در بسیاری از تیم‌ها استاندارد است، چون در `git log --graph` مشخص می‌کند که این تغییرات از یک feature branch جداگانه آمده‌اند.

---
<a name="بخش-11"></a>
## 1️⃣1️⃣ حل تعارض (Merge Conflict)

### تعارض چه زمانی رخ می‌دهد؟
وقتی دو شاخه، **همان خط از همان فایل** را به‌شکل متفاوتی تغییر داده باشند، Git نمی‌داند کدام نسخه درست است و از شما می‌خواهد تصمیم بگیرید.

### چگونه یک تعارض را می‌بینیم؟
هنگام merge، اگر تعارض رخ دهد:
```bash
git merge feature-branch
```
خروجی چیزی شبیه این می‌شود:
```
Auto-merging index.html
CONFLICT (content): Merge conflict in index.html
Automatic merge failed; fix conflicts and then commit the result.
```

### باز کردن فایل درگیر
Git داخل فایل، بخش‌های متعارض را با نشانگرهای خاص علامت‌گذاری می‌کند:

```html
<<<<<<< HEAD
<h1>عنوان نسخه‌ی main</h1>
=======
<h1>عنوان نسخه‌ی feature-branch</h1>
>>>>>>> feature-branch
```

- بین `<<<<<<< HEAD` و `=======` : نسخه‌ی شاخه‌ی فعلی (که روی آن ایستاده‌اید).
- بین `=======` و `>>>>>>> feature-branch` : نسخه‌ی شاخه‌ای که در حال merge کردنش هستید.

### مراحل حل تعارض
1. فایل را در ادیتور باز کنید.
2. تصمیم بگیرید کدام نسخه (یا ترکیبی از هر دو) درست است.
3. نشانگرهای `<<<<<<<`, `=======`, `>>>>>>>` را کاملاً حذف کنید.
4. فایل نهایی را ذخیره کنید.
5. فایل را stage کنید:
```bash
git add index.html
```
6. commit را نهایی کنید (پیام از پیش پر شده است، فقط ذخیره کنید):
```bash
git commit
```

### لغو کامل یک merge در حال انجام (اگر پشیمان شدید)
```bash
git merge --abort
```

### ابزارهای گرافیکی حل تعارض
اگر ترجیح می‌دهید به‌جای متن خام، از رابط گرافیکی استفاده کنید:
```bash
git mergetool
```
(ابزارهایی مثل `vimdiff`, `meld`, یا افزونه‌های VS Code / Neovim مثل `diffview.nvim` را می‌توانید پیکربندی کنید.)

### نکات پیشگیری از تعارض
- شاخه‌های خود را کوچک نگه دارید و مدت طولانی باز نگذارید.
- مرتباً از `main` به شاخه‌ی خودتان merge یا rebase بگیرید تا فاصله زیاد نشود.
- با تیم درباره‌ی فایل‌هایی که هم‌زمان روی آن‌ها کار می‌کنید هماهنگ باشید.

---

<a name="بخش-12"></a>
## 1️⃣2️⃣ Rebase: جایگزین حرفه‌ای Merge

### Rebase چیست؟
به‌جای ساخت یک commit ادغامی جدید (مثل merge)، `rebase` تمام commit‌های شاخه‌ی شما را برمی‌دارد و آن‌ها را **دوباره روی نوک شاخه‌ی مقصد** پخش می‌کند — انگار که از ابتدا از همان‌جا شروع کرده بودید.

```bash
git switch feature-branch
git rebase main
```

### تفاوت بصری Merge در برابر Rebase

**قبل از هر دو عملیات:**
```
main:      A → B → C
                \
feature:         D → E
```

**بعد از Merge:**
```
main:      A → B → C ─────→ M
                \           ↗
feature:         D ─→ E ──┘
```

**بعد از Rebase:**
```
main:      A → B → C
                        \
feature:                 D' → E'   (کپی‌های بازنویسی‌شده‌ی D و E)
```
تاریخچه **خطی و تمیز** می‌شود — انگار هیچ‌وقت شاخه‌ی جداگانه‌ای وجود نداشته.

### چه زمانی از کدام استفاده کنیم؟

| موقعیت | توصیه |
|---|---|
| شاخه‌ی feature شخصی، هنوز push نشده | ✅ Rebase (تاریخچه تمیزتر) |
| شاخه‌ای که با دیگران به اشتراک گذاشته شده | ❌ هرگز rebase نکنید |
| ادغام نهایی یک PR به main | معمولاً Merge (یا Squash — پایین‌تر) |
| می‌خواهید commit‌های شخصی خود را قبل از push مرتب کنید | ✅ Rebase تعاملی |

### ⚠️ قانون طلایی Rebase
> **هرگز شاخه‌ای را که دیگران روی آن کار می‌کنند یا از آن pull گرفته‌اند، rebase نکنید.**
چون تاریخچه بازنویسی می‌شود و SHA همه‌ی commit‌ها عوض می‌شود — باعث سردرگمی و تعارض‌های شدید برای بقیه‌ی تیم می‌شود.

### Interactive Rebase (rebase تعاملی) — ابزار قدرتمند تمیزکاری

```bash
git rebase -i HEAD~3
```
این دستور آخرین ۳ commit را باز می‌کند و به شما اجازه می‌دهد:

```
pick a1b2c3 اضافه کردن فرم لاگین
pick d4e5f6 رفع باگ تایپی
pick g7h8i9 اضافه کردن استایل دکمه
```

گزینه‌های قابل استفاده به‌جای `pick`:
| دستور | عملکرد |
|---|---|
| `pick` | نگه‌داشتن commit بدون تغییر |
| `reword` | نگه‌داشتن commit ولی تغییر پیام آن |
| `edit` | توقف روی این commit برای اصلاح محتوا |
| `squash` | ادغام این commit با commit قبلی (پیام‌ها ترکیب می‌شوند) |
| `fixup` | مثل squash ولی پیام این commit دور انداخته می‌شود |
| `drop` | حذف کامل این commit |

مثال کاربردی: فرض کنید ۳ commit کوچک برای رفع تایپو دارید که می‌خواهید همه را در یک commit تمیز ادغام کنید:
```
pick a1b2c3 اضافه کردن فرم لاگین
fixup d4e5f6 رفع باگ تایپی
fixup g7h8i9 اضافه کردن استایل دکمه
```
نتیجه: فقط یک commit با پیام "اضافه کردن فرم لاگین" باقی می‌ماند که شامل هر سه تغییر است.

### حل تعارض در حین Rebase
اگر در میانه‌ی rebase تعارض رخ دهد:
```bash
# فایل را دستی اصلاح کنید، سپس:
git add filename
git rebase --continue

# یا برای لغو کامل عملیات:
git rebase --abort
```

---
<a name="بخش-13"></a>
## 1️⃣3️⃣ کار با ریموت‌ها (GitHub, GitLab و...)

### Remote چیست؟
یک remote، آدرس یک نسخه‌ی دیگر از ریپازیتوری شماست که معمولاً روی یک سرور (مثل GitHub) قرار دارد. وقتی یک پروژه را clone می‌کنید، Git به‌طور خودکار یک remote به نام `origin` می‌سازد که به آدرس اصلی اشاره می‌کند.

### دیدن remote‌های فعلی
```bash
git remote -v
```

### اضافه کردن یک remote جدید (وقتی از صفر با git init شروع کرده‌اید)
```bash
git remote add origin https://github.com/username/repo-name.git
```

### تغییر آدرس یک remote
```bash
git remote set-url origin https://github.com/username/new-repo.git
```

### حذف یک remote
```bash
git remote remove origin
```

### ساخت ریپازیتوری در GitHub و اتصال پروژه‌ی محلی به آن (گام‌به‌گام)
```bash
# ۱. داخل GitHub یک ریپازیتوری خالی جدید بسازید (بدون README اگر پروژه از قبل دارید)
# ۲. در پروژه‌ی لوکال:
git init
git add .
git commit -m "کامیت اولیه"
git branch -M main
git remote add origin https://github.com/username/repo-name.git
git push -u origin main
```

---

<a name="بخش-14"></a>
## 1️⃣4️⃣ Clone، Fetch، Pull، Push

### Clone — دانلود کامل یک پروژه
```bash
git clone https://github.com/username/repo.git
```

### Fetch — دریافت تغییرات remote **بدون** ادغام آن‌ها در کد شما
```bash
git fetch origin
```
این دستور فقط اطلاعات جدید را دانلود می‌کند (مثل شاخه‌ها و commit‌های جدید روی سرور) ولی فایل‌های working directory شما را دست‌نمی‌زند. برای دیدن این تغییرات:
```bash
git log origin/main
git diff main origin/main
```

### Pull — دریافت **و** ادغام خودکار تغییرات remote
```bash
git pull origin main
```
معادل است با:
```bash
git fetch origin
git merge origin/main
```

**نسخه‌ی جایگزین با rebase به‌جای merge (تاریخچه‌ی تمیزتر):**
```bash
git pull --rebase origin main
```

💡 توصیه: بسیاری از تیم‌های حرفه‌ای، `pull.rebase` را به‌صورت پیش‌فرض فعال می‌کنند:
```bash
git config --global pull.rebase true
```

### Push — ارسال commit‌های لوکال به remote
```bash
git push origin main
```

اولین بار که یک شاخه‌ی جدید را push می‌کنید، باید ارتباط آن با remote را تنظیم کنید (upstream):
```bash
git push -u origin feature-branch
```
پس از این تنظیم، دفعات بعد کافی است فقط بنویسید:
```bash
git push
```

### Push اجباری (⚠️ بسیار خطرناک — با احتیاط شدید استفاده کنید)
اگر تاریخچه‌ی لوکال را بازنویسی کرده‌اید (مثلاً بعد از rebase) و می‌خواهید آن را جایگزین نسخه‌ی remote کنید:
```bash
git push --force
```
⚠️ این دستور می‌تواند کار همکارانتان را از بین ببرد. نسخه‌ی امن‌تر:
```bash
git push --force-with-lease
```
که فقط زمانی force می‌کند که هیچ‌کس دیگری از زمان آخرین fetch شما، چیزی push نکرده باشد.

---

<a name="بخش-15"></a>
## 1️⃣5️⃣ Git Stash

### چه زمانی به Stash نیاز داریم؟
فرض کنید وسط کار روی یک قابلیت هستید (تغییرات نیمه‌کاره در working directory) و ناگهان باید سریع به یک شاخه‌ی دیگر بروید (مثلاً برای رفع یک باگ فوری). نمی‌خواهید تغییرات نیمه‌کاره را commit کنید، ولی هم نمی‌توانید شاخه عوض کنید چون Git اجازه نمی‌دهد تغییرات موجود از بین بروند.

**راه‌حل: `stash` — یک «قفسه‌ی موقت» برای تغییرات**

### ذخیره‌ی تغییرات در stash
```bash
git stash
# یا با پیام توضیحی:
git stash save "کار نیمه‌تمام روی فرم لاگین"
```
working directory تمیز می‌شود، انگار تغییری نداده‌اید (بازمی‌گردد به آخرین commit).

### دیدن لیست stash‌ها
```bash
git stash list
```
خروجی نمونه:
```
stash@{0}: On feature-login: کار نیمه‌تمام روی فرم لاگین
stash@{1}: On main: تغییرات آزمایشی CSS
```

### بازگرداندن آخرین stash
```bash
git stash pop
```
تغییرات را برمی‌گرداند **و** آن را از لیست stash حذف می‌کند.

اگر می‌خواهید تغییرات را برگردانید ولی در لیست stash هم باقی بماند:
```bash
git stash apply
```

### بازگرداندن یک stash خاص (نه لزوماً آخرین)
```bash
git stash apply stash@{1}
```

### حذف یک stash بدون اعمال آن
```bash
git stash drop stash@{0}
```

### پاک کردن کامل همه‌ی stash‌ها
```bash
git stash clear
```

### شامل کردن فایل‌های untracked در stash
```bash
git stash -u
```

---

<a name="بخش-16"></a>
## 1️⃣6️⃣ برچسب‌ها (Tags)

### Tag چیست و چرا کاربردی است؟
Tag یک اشاره‌گر دائمی به یک commit خاص است — معمولاً برای علامت‌گذاری نسخه‌های رسمی انتشار (مثل `v1.0.0`).

### دو نوع Tag

**۱. Lightweight Tag (سبک — فقط یک اسم برای یک commit)**
```bash
git tag v1.0.0
```

**۲. Annotated Tag (حاشیه‌دار — شامل نویسنده، تاریخ، پیام؛ توصیه‌شده برای انتشارها)**
```bash
git tag -a v1.0.0 -m "اولین نسخه‌ی پایدار"
```

### تگ زدن به یک commit قدیمی‌تر (نه آخرین commit)
```bash
git tag -a v0.9.0 a3f5c9d -m "نسخه‌ی بتا"
```

### دیدن لیست تگ‌ها
```bash
git tag
git tag -l "v1.*"    # فیلتر با الگو
```

### دیدن جزئیات یک تگ
```bash
git show v1.0.0
```

### ارسال تگ‌ها به remote (به‌طور پیش‌فرض push نمی‌شوند!)
```bash
git push origin v1.0.0        # یک تگ خاص
git push origin --tags        # همه‌ی تگ‌ها
```

### حذف یک تگ
```bash
git tag -d v1.0.0                     # حذف لوکال
git push origin --delete v1.0.0       # حذف از remote
```

---
<a name="بخش-17"></a>
## 1️⃣7️⃣ Cherry-pick

### چه زمانی به آن نیاز داریم؟
فرض کنید یک commit خاص روی یک شاخه‌ی دیگر ساخته‌اید (مثلاً یک رفع باگ مهم) و می‌خواهید **فقط همان یک commit** را — بدون کل شاخه — به شاخه‌ی فعلی خودتان بیاورید.

```bash
git switch main
git cherry-pick a3f5c9d
```

commit مورد نظر (با همان تغییرات) روی شاخه‌ی فعلی شما اعمال می‌شود، ولی با یک SHA جدید (چون والدش فرق کرده).

### Cherry-pick چند commit با هم
```bash
git cherry-pick a3f5c9d d4e5f6g
```

### در صورت بروز تعارض
```bash
# فایل را اصلاح کنید، سپس:
git add filename
git cherry-pick --continue

# یا لغو کامل:
git cherry-pick --abort
```

### موارد استفاده‌ی رایج
- انتقال یک hotfix فوری از شاخه‌ی توسعه به شاخه‌ی production.
- بازیابی یک commit مفید که اشتباهاً روی شاخه‌ی اشتباه ساخته شده.

---

<a name="بخش-18"></a>
## 1️⃣8️⃣ Reflog: ماشین زمان Git

### Reflog چیست؟
`reflog` (reference log) یک ثبت‌کننده‌ی داخلی است که **هر حرکت** HEAD در ریپازیتوری شما را ذخیره می‌کند — حتی commit‌هایی که با `reset --hard` یا rebase «گم» شده‌اند. این یعنی **تقریباً هیچ اشتباهی در Git واقعاً غیرقابل بازگشت نیست.**

```bash
git reflog
```

خروجی نمونه:
```
a3f5c9d HEAD@{0}: commit: اضافه کردن فرم لاگین
d4e5f6g HEAD@{1}: reset: moving to HEAD~1
g7h8i9j HEAD@{2}: commit: رفع باگ مهم (این commit "گم شده" بود!)
```

### بازیابی یک commit "گم‌شده"
فرض کنید اشتباهاً `git reset --hard HEAD~2` زدید و یک commit مهم را از دست دادید:
```bash
git reflog                       # پیدا کردن SHA کامیت گم‌شده
git reset --hard g7h8i9j         # بازگشت به آن نقطه
# یا اگر فقط می‌خواهید commit را برگردانید بدون تغییر موقعیت فعلی:
git cherry-pick g7h8i9j
```

💡 **نکته‌ی امیدبخش**: reflog معمولاً به مدت ۹۰ روز داده‌های commit‌های حذف‌شده را نگه می‌دارد (حتی اگر از تاریخچه‌ی عادی `git log` دیگر دیده نشوند). پس اگر با یک دستور خطرناک اشتباهی کردید، **قبل از هر کاری وحشت نکنید** — سراغ reflog بروید.

---

<a name="بخش-19"></a>
## 1️⃣9️⃣ Bisect: پیدا کردن باگ با جست‌وجوی دودویی

### مسئله
یک باگ در کد پیدا کرده‌اید که مطمئنید قبلاً وجود نداشت، ولی نمی‌دانید دقیقاً **کدام یکی از صدها commit اخیر** آن را ایجاد کرده. بررسی دستی همه‌ی آن‌ها زمان‌بر است.

### راه‌حل: جست‌وجوی دودویی خودکار
```bash
git bisect start
git bisect bad                    # commit فعلی خراب است
git bisect good v1.0.0             # این نسخه‌ی قدیمی سالم بود
```

Git خودکار شما را به یک commit **دقیقاً وسط** این بازه منتقل می‌کند. کد را تست می‌کنید و به Git می‌گویید نتیجه چه بود:
```bash
git bisect good     # اگر این نسخه سالم بود
# یا
git bisect bad      # اگر این نسخه هم خراب بود
```

Git این فرآیند را با نصف کردن مداوم بازه تکرار می‌کند تا دقیقاً commit مقصر را پیدا کند (برای ۱۰۰۰ commit، فقط حدود ۱۰ مرحله لازم است — چون جست‌وجو لگاریتمی است).

### پایان جست‌وجو
```bash
git bisect reset     # بازگشت به وضعیت اولیه پس از پیدا کردن commit مقصر
```

### خودکارسازی کامل با یک اسکریپت تست
اگر یک اسکریپت دارید که به‌طور خودکار می‌گوید کد سالم است یا نه:
```bash
git bisect start HEAD v1.0.0
git bisect run ./test-script.sh
```

---

<a name="بخش-20"></a>
## 2️⃣0️⃣ Submodules

### Submodule چیست؟
گاهی پروژه‌ی شما به یک ریپازیتوری Git **دیگر** وابسته است (مثلاً یک کتابخانه‌ی مشترک). Submodule به شما اجازه می‌دهد آن ریپازیتوری را به‌عنوان یک زیرپوشه در پروژه‌ی خود «جاسازی» کنید، در حالی که تاریخچه‌ی Git آن مستقل باقی می‌ماند.

### اضافه کردن یک submodule
```bash
git submodule add https://github.com/username/library.git libs/library
```

### Clone کردن پروژه‌ای که submodule دارد
```bash
git clone --recurse-submodules https://github.com/username/main-project.git
```
اگر قبلاً بدون این فلگ clone کرده‌اید:
```bash
git submodule update --init --recursive
```

### به‌روزرسانی submodule به آخرین نسخه
```bash
git submodule update --remote
```

⚠️ نکته: Submodules قدرتمند ولی نسبتاً پیچیده هستند و بسیاری از تیم‌های مدرن به‌جای آن از ابزارهایی مثل **package manager**ها (npm, pip, cargo) یا **monorepo**ها استفاده می‌کنند.

---

<a name="بخش-21"></a>
## 2️⃣1️⃣ Git Hooks

### Hook چیست؟
اسکریپت‌هایی که Git به‌طور خودکار در لحظات خاصی از چرخه‌ی کار اجرا می‌کند — مثلاً «درست قبل از commit» یا «بعد از push». برای اجرای خودکار تست‌ها، بررسی فرمت کد، یا جلوگیری از commit کردن اشتباهات رایج استفاده می‌شوند.

### محل قرارگیری
داخل هر ریپازیتوری: `.git/hooks/`
(این پوشه به‌طور پیش‌فرض شامل نمونه‌های `.sample` است که باید rename و اجرایی شوند.)

### نمونه‌های رایج
| Hook | زمان اجرا |
|---|---|
| `pre-commit` | درست قبل از ثبت commit — عالی برای اجرای linter یا formatter |
| `commit-msg` | برای اعتبارسنجی فرمت پیام commit |
| `pre-push` | قبل از push — عالی برای اجرای تست‌ها |
| `post-merge` | بعد از merge — مثلاً برای نصب خودکار dependency جدید |

### مثال ساده‌ی pre-commit (جلوگیری از commit کردن فایل‌های حاوی `console.log`)

فایل `.git/hooks/pre-commit`:
```bash
#!/bin/sh
if git diff --cached | grep -q "console.log"; then
  echo "❌ خطا: console.log در کد پیدا شد. لطفاً قبل از commit حذف کنید."
  exit 1
fi
```
سپس آن را اجرایی کنید:
```bash
chmod +x .git/hooks/pre-commit
```

### مشکل اشتراک‌گذاری Hooks
پوشه‌ی `.git/hooks/` به‌طور پیش‌فرض commit **نمی‌شود** (چون داخل `.git/` است). برای اشتراک‌گذاری hooks بین اعضای تیم، از ابزارهایی مانند **Husky** (برای پروژه‌های Node.js) یا **pre-commit** (فریم‌ورک پایتونی مستقل از زبان) استفاده کنید.

---
<a name="بخش-22"></a>
## 2️⃣2️⃣ درون Git چه خبر است؟ (معماری داخلی)

درک این بخش، فهم شما از **همه‌ی** دستورات قبلی را عمیق‌تر می‌کند و به شما اعتماد‌به‌نفس می‌دهد که Git «جادو» نیست.

### چهار نوع شیء (Object) در Git

Git در اصل یک **پایگاه‌داده‌ی key-value** است. هر «شیء» با یک هش SHA-1 (۴۰ کاراکتری) شناسایی می‌شود که از **محتوای** آن شیء محاسبه می‌شود (نه از نامش).

| نوع شیء | نقش |
|---|---|
| **Blob** | محتوای خام یک فایل (بدون نام یا مسیر) |
| **Tree** | مثل یک پوشه؛ لیستی از blob‌ها و tree‌های دیگر با نام و دسترسی‌هایشان |
| **Commit** | اشاره‌گر به یک tree (وضعیت کامل پروژه)، به‌همراه نویسنده، پیام، تاریخ، و اشاره به commit(های) والد |
| **Tag** | اشاره‌گر دائمی و نام‌گذاری‌شده به یک commit خاص |

### تصویر ساده از رابطه‌ی این اشیاء
```
Commit
  ├── author, date, message
  ├── parent commit(s)
  └── → Tree (ریشه‌ی پروژه)
         ├── README.md → Blob
         ├── src/ → Tree
         │      ├── main.py → Blob
         │      └── utils.py → Blob
         └── ...
```

### دیدن اشیاء داخلی به‌صورت خام (برای کنجکاوها)
```bash
git cat-file -p HEAD              # محتوای آخرین commit
git cat-file -t a3f5c9d           # نوع یک شیء (blob/tree/commit/tag)
git ls-tree HEAD                  # محتویات tree ریشه
```

### HEAD چیست دقیقاً؟
`HEAD` یک اشاره‌گر است که معمولاً به آخرین commit روی شاخه‌ی فعلی اشاره می‌کند. در واقع فایلی به همین نام در `.git/HEAD` وجود دارد که محتوایش چیزی شبیه این است:
```
ref: refs/heads/main
```
یعنی HEAD خودش به یک شاخه اشاره می‌کند، و آن شاخه به یک commit. به همین دلیل وقتی commit جدید می‌سازید، شاخه (و درنتیجه HEAD) به‌طور خودکار جلو می‌رود.

### حالت Detached HEAD
اگر مستقیماً به یک commit خاص (نه یک شاخه) checkout کنید:
```bash
git checkout a3f5c9d
```
وارد حالت «Detached HEAD» می‌شوید — یعنی HEAD دیگر به هیچ شاخه‌ای متصل نیست، بلکه مستقیماً به آن commit اشاره می‌کند. اگر در این حالت commit جدیدی بسازید و بدون ساختن شاخه از آن خارج شوید، آن commit ممکن است «گم» شود (هرچند از طریق reflog قابل بازیابی است).

برای امن ماندن، اگر می‌خواهید از یک commit قدیمی کار جدیدی شروع کنید:
```bash
git checkout -b new-branch-name a3f5c9d
```

---

<a name="بخش-23"></a>
## 2️⃣3️⃣ استانداردهای پیام کامیت (Commit Message)

### چرا مهم است؟
پیام‌های commit بد (`fix`, `update`, `asdf`) پروژه را برای همه — از جمله خودتان در آینده — غیرقابل‌فهم می‌کنند. پیام‌های خوب، تاریخچه‌ی پروژه را به یک **مستند زنده** تبدیل می‌کنند.

### قانون‌های پایه‌ای یک پیام خوب
1. خط اول (subject): حداکثر ۵۰ کاراکتر، فعل امری، خلاصه‌ی دقیق.
2. یک خط خالی بعد از subject.
3. بدنه (body، اختیاری): توضیح **چرا** این تغییر انجام شد، نه فقط چه چیزی.

مثال:
```
افزودن اعتبارسنجی ایمیل در فرم ثبت‌نام

قبلاً کاربران می‌توانستند ایمیل نامعتبر وارد کنند که باعث
شکست ارسال ایمیل تأیید می‌شد. این تغییر یک regex ساده
برای بررسی فرمت ایمیل قبل از ارسال فرم اضافه می‌کند.

Fixes #142
```

### استاندارد Conventional Commits (بسیار توصیه‌شده و رایج در صنعت)

فرمت:
```
<type>(<scope optional>): <subject>
```

انواع رایج:
| Type | استفاده |
|---|---|
| `feat` | افزودن قابلیت جدید |
| `fix` | رفع باگ |
| `docs` | تغییر فقط در مستندات |
| `style` | تغییرات فرمتی (بدون تأثیر روی منطق کد) |
| `refactor` | بازنویسی کد بدون تغییر رفتار |
| `test` | افزودن یا اصلاح تست‌ها |
| `chore` | کارهای جانبی (به‌روزرسانی dependency و...) |
| `perf` | بهبود کارایی |

مثال‌ها:
```
feat(auth): افزودن ورود با گوگل
fix(navbar): رفع مشکل همپوشانی منو در موبایل
docs(readme): به‌روزرسانی راهنمای نصب
refactor(api): ساده‌سازی توابع دریافت داده
```

مزیت بزرگ این استاندارد: امکان **تولید خودکار Changelog** و تعیین نسخه (Semantic Versioning) بر اساس نوع commit‌ها، با ابزارهایی مثل `semantic-release`.

---
<a name="بخش-24"></a>
## 2️⃣4️⃣ ورک‌فلوهای حرفه‌ای تیمی

### چرا به یک ورک‌فلو استاندارد نیاز داریم؟
بدون قوانین مشخص درباره‌ی این‌که «چه زمانی و چگونه شاخه بسازیم، merge کنیم و منتشر کنیم»، تاریخچه‌ی پروژه سریعاً آشفته و غیرقابل مدیریت می‌شود. تیم‌های حرفه‌ای از یکی از این الگوهای شناخته‌شده استفاده می‌کنند:

### الگو ۱: GitHub Flow (ساده، سریع، مناسب اکثر پروژه‌های وب و استارتاپ‌ها)

```
main (همیشه قابل استقرار/deploy است)
  │
  ├── feature/login-page  → توسعه → Pull Request → Review → Merge به main
  ├── bugfix/navbar-fix    → توسعه → Pull Request → Review → Merge به main
```

قوانین:
1. `main` همیشه باید در وضعیت قابل انتشار باشد.
2. هر قابلیت جدید در یک شاخه‌ی جداگانه (`feature/...`) توسعه داده می‌شود.
3. وقتی آماده شد، یک **Pull Request** باز می‌کنید.
4. بعد از بررسی و تأیید تیم، merge به `main` انجام می‌شود.
5. بلافاصله پس از merge، معمولاً به‌صورت خودکار deploy می‌شود.

### الگو ۲: Git Flow (رسمی‌تر، مناسب پروژه‌های بزرگ با چرخه‌ی انتشار مشخص)

شاخه‌های اصلی:
- `main` → فقط نسخه‌های رسمی منتشرشده
- `develop` → آخرین کد در حال توسعه (پایه‌ی همه‌ی feature‌ها)
- `feature/*` → از `develop` جدا می‌شود، به `develop` برمی‌گردد
- `release/*` → آماده‌سازی نهایی قبل از انتشار (رفع باگ‌های جزئی، نه قابلیت جدید)
- `hotfix/*` → رفع فوری باگ روی نسخه‌ی منتشرشده (مستقیماً از `main` جدا می‌شود)

```
main:      ─────●──────────────●───────── (فقط نسخه‌های رسمی: v1.0, v1.1)
                 │              │
release:         ●──●──●───────┘
                 │
develop:  ●──●───●──●──●──●──●──●──
              │           │
feature:      ●──●────────┘
```

این الگو پیچیده‌تر است ولی برای پروژه‌هایی با انتشار نسخه‌های زمان‌بندی‌شده (مثل نرم‌افزارهای دسکتاپ یا موبایل) بسیار مناسب است.

### الگو ۳: Trunk-Based Development (مدرن، محبوب در تیم‌های با استقرار مداوم/CI-CD قوی)

- تقریباً همه مستقیماً روی `main` (یا شاخه‌های بسیار کوتاه‌عمر، کمتر از یک روز) کار می‌کنند.
- به‌جای شاخه‌های بلندمدت، از **Feature Flags** برای پنهان کردن قابلیت‌های ناتمام استفاده می‌شود.
- نیازمند تست‌های خودکار قوی و فرهنگ commit‌های کوچک و مکرر است.

### کدام را انتخاب کنیم؟
| نوع پروژه | ورک‌فلوی پیشنهادی |
|---|---|
| پروژه‌ی شخصی یا تیم کوچک | GitHub Flow |
| پروژه‌ی بزرگ با نسخه‌های رسمی زمان‌بندی‌شده | Git Flow |
| تیم بزرگ با CI/CD بسیار قوی | Trunk-Based Development |

---

<a name="بخش-25"></a>
## 2️⃣5️⃣ Pull Request و بررسی کد (Code Review)

### Pull Request (PR) چیست؟
یک PR درخواستی است که می‌گوید: «من این تغییرات را در شاخه‌ی X ساخته‌ام، لطفاً بررسی کنید و در صورت تأیید، آن‌ها را در شاخه‌ی Y (معمولاً main) ادغام کنید.» PR در سطح GitHub/GitLab است، نه یک مفهوم خام Git.

### گردش کار معمول PR
```bash
git switch -c feature/dark-mode
# ... کدنویسی و commit ...
git push -u origin feature/dark-mode
```
سپس در وب‌سایت GitHub، وارد ریپازیتوری شوید و روی «Compare & pull request» کلیک کنید.

### نوشتن یک توضیح خوب برای PR
یک PR خوب باید شامل باشد:
- **عنوان واضح**: چه چیزی تغییر کرده.
- **توضیح**: چرا این تغییر لازم بود، چه رویکردی گرفته شده.
- **نحوه‌ی تست**: چگونه می‌توان این تغییر را تست کرد.
- **اسکرین‌شات** (در صورت تغییرات UI).
- ارجاع به issue مرتبط (مثلاً `Closes #45`).

### بررسی کد (Code Review) — نکات برای بررسی‌کننده
- روی **منطق و طراحی** تمرکز کنید، نه فقط سلیقه‌ی شخصی در استایل کد.
- سؤال بپرسید، دستور ندهید («چرا این رویکرد را انتخاب کردی؟» بهتر از «این اشتباه است»).
- نکات مثبت را هم اشاره کنید، نه فقط ایرادها.

### گزینه‌های ادغام یک PR در GitHub

| گزینه | نتیجه |
|---|---|
| **Merge commit** | یک merge commit عادی ساخته می‌شود؛ تمام commit‌های شاخه حفظ می‌شوند |
| **Squash and merge** | همه‌ی commit‌های شاخه در **یک** commit تمیز ادغام می‌شوند |
| **Rebase and merge** | commit‌های شاخه به‌صورت خطی روی main پخش می‌شوند (بدون merge commit) |

💡 توصیه‌ی رایج: برای شاخه‌های feature با commit‌های نامرتب («wip», «fix typo»)، از **Squash and merge** استفاده کنید تا تاریخچه‌ی main تمیز بماند.

### حل تعارض قبل از merge کردن PR
اگر شاخه‌ی `main` از زمان باز شدن PR جلوتر رفته باشد:
```bash
git switch feature/dark-mode
git fetch origin
git merge origin/main
# یا برای تاریخچه‌ی تمیزتر:
git rebase origin/main
# سپس در صورت rebase:
git push --force-with-lease
```

---
<a name="بخش-26"></a>
## 2️⃣6️⃣ آشنایی با GitHub Actions (CI/CD مقدماتی)

### چرا این را در یک دوره‌ی Git می‌آوریم؟
GitHub Actions مستقیماً با ریپازیتوری Git شما یکپارچه است و به شما اجازه می‌دهد کارهایی مثل «هر بار که push می‌کنم، تست‌ها را خودکار اجرا کن» را انجام دهید. دانستن این مفهوم، تصویر کامل کار حرفه‌ای با Git/GitHub را کامل می‌کند.

### ساختار پایه
یک فایل YAML در مسیر `.github/workflows/` قرار می‌گیرد. مثال ساده برای اجرای خودکار تست‌های پایتون در هر push:

```yaml
name: اجرای تست‌ها

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: نصب پایتون
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: نصب وابستگی‌ها
        run: pip install -r requirements.txt
      - name: اجرای تست‌ها
        run: pytest
```

### چه اتفاقی می‌افتد؟
هر بار که به `main` push کنید یا یک PR باز کنید، GitHub به‌طور خودکار این workflow را اجرا می‌کند و نتیجه (✅ موفق یا ❌ ناموفق) را مستقیماً روی صفحه‌ی PR نمایش می‌دهد — قبل از این‌که کد اصلاً merge شود.

### مزیت برای ریپازیتوری‌های آموزشی/متن‌باز
اضافه کردن یک بج (badge) وضعیت به README که وضعیت build را نشان دهد:
```markdown
![tests](https://github.com/username/repo/actions/workflows/test.yml/badge.svg)
```

---

<a name="بخش-27"></a>
## 2️⃣7️⃣ دستورات و تنظیمات پیشرفته و کاربردی

### دیدن یک فایل از یک commit یا شاخه‌ی دیگر بدون checkout کردن
```bash
git show main:path/to/file.txt
```

### آوردن یک فایل خاص از یک شاخه‌ی دیگر به شاخه‌ی فعلی
```bash
git checkout feature-branch -- path/to/file.txt
```

### جست‌وجوی متن در کل تاریخچه‌ی پروژه
```bash
git log -S "نام تابع خاص" --oneline
```
این دستور commit‌هایی را پیدا می‌کند که تعداد رخدادهای یک رشته‌ی متنی خاص را تغییر داده‌اند (خیلی مفید برای پیدا کردن این‌که یک تابع کِی اضافه یا حذف شده).

### شمارش تعداد commit‌ها
```bash
git rev-list --count HEAD
```

### دیدن آمار مشارکت هر نویسنده
```bash
git shortlog -sn
```

### فشرده‌سازی و بهینه‌سازی ریپازیتوری (برای پروژه‌های قدیمی و بزرگ)
```bash
git gc
```

### Worktree — کار همزمان روی چند شاخه بدون stash یا clone مجدد
```bash
git worktree add ../project-hotfix hotfix-branch
```
این دستور یک پوشه‌ی جدید کاملاً مستقل می‌سازد که به شاخه‌ی دیگری از **همان** ریپازیتوری اشاره می‌کند — بدون نیاز به stash کردن کار فعلی یا clone دوباره‌ی کل پروژه.

### Sparse Checkout — دانلود فقط بخشی از یک ریپازیتوری بزرگ
```bash
git clone --no-checkout https://github.com/username/huge-repo.git
cd huge-repo
git sparse-checkout init --cone
git sparse-checkout set src/frontend
git checkout main
```

### امضای دیجیتال commit‌ها با GPG (برای اثبات هویت نویسنده)
```bash
git config --global commit.gpgsign true
git config --global user.signingkey YOUR_GPG_KEY_ID
```

---

<a name="بخش-28"></a>
## 2️⃣8️⃣ اشتباهات رایج و راه‌حل آن‌ها

### ❌ «اشتباهی روی main کار کردم، می‌خواستم روی یک branch جدید باشم»
```bash
# قبل از commit کردن:
git switch -c correct-branch-name
# حالا تغییرات همراه شما به شاخه‌ی جدید منتقل می‌شوند و main دست‌نخورده می‌ماند
```

### ❌ «فایل بزرگی را اشتباهی commit کردم و push کردم»
```bash
# برای commit آخر که هنوز push نشده:
git reset --soft HEAD~1
git restore --staged large-file.zip
git commit -m "پیام درست بدون فایل بزرگ"

# اگر قبلاً push شده، نیاز به ابزارهای پیشرفته‌تری مثل
# git filter-repo یا BFG Repo-Cleaner دارید (پیشنهاد: جست‌وجوی مستندات رسمی)
```

### ❌ «پیام آخرین commit اشتباه تایپی دارد»
```bash
git commit --amend -m "پیام صحیح"
# فقط اگر هنوز push نکرده‌اید!
```

### ❌ «commit را روی شاخه‌ی اشتباه زدم»
```bash
# فرض کنید روی main بودید ولی باید روی feature-branch می‌بودید
git log                          # پیدا کردن SHA کامیت اشتباه
git reset --hard HEAD~1          # حذف commit از main (مراقب باشید!)
git switch feature-branch
git cherry-pick <SHA-ی commit اشتباه>
```

### ❌ «یک فایل حساس (رمز عبور) را اشتباهی commit و push کردم»
1. فوراً رمز/کلید را در سرویس مربوطه **باطل و تعویض کنید** (این مهم‌ترین قدم است — حتی اگر از تاریخچه پاکش کنید، ممکن است کسی قبلاً دیده باشد).
2. سپس با `git filter-repo` یا BFG آن را از کل تاریخچه پاک کنید.
3. `.gitignore` را برای جلوگیری از تکرار به‌روزرسانی کنید.

### ❌ «نمی‌دانم الان چند تا commit جلوتر یا عقب‌تر از main هستم»
```bash
git status                              # اگر شاخه‌ی upstream تنظیم باشد نشان می‌دهد
git log --oneline main..feature-branch  # commit‌های فقط در feature-branch
git log --oneline feature-branch..main  # commit‌های فقط در main
```

### ❌ «Merge را شروع کردم ولی پشیمان شدم»
```bash
git merge --abort
```

### ❌ «Rebase را شروع کردم ولی گیر کردم»
```bash
git rebase --abort
```

---
<a name="بخش-29"></a>
## 2️⃣9️⃣ Cheat Sheet نهایی (مرجع سریع)

### شروع کار
```bash
git init                          # ساخت ریپازیتوری جدید
git clone <url>                    # کپی یک ریپازیتوری موجود
```

### چرخه‌ی اصلی
```bash
git status                         # وضعیت فعلی
git add <file>                     # افزودن به staging
git add .                          # افزودن همه
git commit -m "پیام"                # ثبت commit
git log --oneline --graph          # تاریخچه‌ی خلاصه
```

### شاخه‌ها
```bash
git branch                         # لیست شاخه‌ها
git switch -c <name>               # ساخت و جابه‌جایی
git switch <name>                  # جابه‌جایی
git merge <branch>                 # ادغام
git branch -d <name>               # حذف شاخه
```

### همگام‌سازی با Remote
```bash
git remote add origin <url>
git push -u origin main
git push
git pull
git fetch
```

### لغو و بازگردانی
```bash
git restore <file>                 # لغو تغییرات working directory
git restore --staged <file>        # خروج از staging
git reset --soft HEAD~1            # لغو commit، حفظ تغییرات staged
git reset --hard HEAD~1            # ⚠️ حذف کامل آخرین commit و تغییرات
git revert <commit>                # لغو امن با commit جدید
git commit --amend                 # اصلاح آخرین commit
```

### ابزارهای پیشرفته
```bash
git stash / git stash pop          # ذخیره و بازیابی موقت تغییرات
git rebase -i HEAD~n                # rebase تعاملی
git cherry-pick <commit>            # انتقال یک commit خاص
git reflog                          # تاریخچه‌ی کامل حرکات HEAD
git bisect start                    # جست‌وجوی دودویی برای باگ
git tag -a v1.0.0 -m "پیام"         # ساخت تگ نسخه
```

### بررسی و مقایسه
```bash
git diff                            # تغییرات unstaged
git diff --staged                   # تغییرات staged
git show <commit>                   # جزئیات یک commit
git blame <file>                    # نویسنده‌ی هر خط
```

---

<a name="بخش-30"></a>
## 3️⃣0️⃣ منابع بیشتر برای یادگیری عمیق‌تر

> ⚠️ توجه: ممکن است جزئیات لینک‌ها یا محتوای دقیق منابع زیر را اشتباه به خاطر بیاورم. قبل از اتکای کامل، آدرس‌ها را خودتان بررسی کنید.

- **مستندات رسمی Git** — کامل‌ترین و دقیق‌ترین منبع: [git-scm.com/doc](https://git-scm.com/doc)
- **کتاب رایگان Pro Git** (نوشته‌ی Scott Chacon) — به‌صورت کامل و رایگان آنلاین: [git-scm.com/book](https://git-scm.com/book)
- **Oh My Git!** — یک بازی تعاملی برای یادگیری Git با تصویرسازی گراف commit‌ها.
- **Learn Git Branching** — شبیه‌ساز آنلاین تعاملی برای تمرین branching و rebase.
- مستندات رسمی **GitHub Docs** برای یادگیری Actions، Pull Requestها و تنظیمات ریپازیتوری.

### پیشنهاد مسیر تمرین عملی
1. یک پروژه‌ی شخصی کوچک بسازید و کل این دوره را روی آن پیاده‌سازی کنید.
2. عمداً یک conflict بسازید (دو شاخه که یک خط را متفاوت تغییر داده‌اند) و آن را دستی حل کنید.
3. یک اشتباه عمدی بزنید (مثلاً `reset --hard` روی یک commit مهم) و با `reflog` آن را نجات دهید.
4. یک ریپازیتوری متن‌باز واقعی روی GitHub پیدا کنید، آن را fork کنید، یک تغییر کوچک ایجاد کنید و یک Pull Request واقعی بفرستید.

---

**ساخته‌شده با ❤️ برای جامعه‌ی فارسی‌زبان برنامه‌نویسی**
